# Redis 

* `1 start server`

    ```bash
    redis-server
    ```

  * `2 start CLI`

    ```bash
    redis-cli
    ```
* `3 CRUD`

    * ko quan trong in hoa hay thuong
  * `Create - read  - update - delete`  
    * `set  - get   - update - del`  

    ```bash
    set [key] [value]
    SET or
    setex [key] [seconds] [value]
    or setEx 
    ```
